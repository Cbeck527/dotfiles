{ config, pkgs, ... }:

# This is an example local.nix file
# Copy this to local.nix and customize for machine-specific settings
# local.nix is gitignored and should contain secrets or machine-specific config

{
  # Example: Add extra packages not in common/personal/work modules
  # home.packages = with pkgs; [
  #   some-package
  # ];

  # Example: Override git email for work machine
  # programs.git = {
  #   enable = true;
  #   userEmail = "work.email@company.com";
  # };

  # Example: Add environment variables
  # home.sessionVariables = {
  #   SOME_SECRET_KEY = "secret-value";
  # };

  # Example: Add SSH config
  # programs.ssh = {
  #   enable = true;
  #   matchBlocks = {
  #     "work-server" = {
  #       hostname = "internal.company.com";
  #       user = "myuser";
  #     };
  #   };
  # };
}
